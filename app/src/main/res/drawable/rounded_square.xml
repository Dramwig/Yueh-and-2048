<?xml version="1.0" encoding="utf-8"?>
<!-- 用于 game_box_view -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充颜色 -->
    <solid android:color="@color/game_box_background"></solid>

    <!-- 矩形边框线的宽度，颜色灰色 -->
    <stroke android:width="@dimen/game_box_frame" android:color="@color/game_box_frame"></stroke>

    <!-- 矩形的圆角半径 -->
<!--    <corners android:radius="@dimen/game_box_radius"/>-->
    <corners android:radius="100px"/>
</shape>